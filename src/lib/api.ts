// import { IWatch } from "@/interfaces";
// import { mockData } from "@/mock/watch";

// export async function getWatchBySlug(slug: string): Promise<IWatch> {
//   const watch = mockData.find((w) => w.slug === slug);
//   if (!watch) {
//     throw new Error(`Watch with slug "${slug}" not found`);
//   }
//   return watch;
// }

// export async function getAllWatchSlugs(): Promise<string[]> {
//   return mockData.map((w) => w.slug);
// }
